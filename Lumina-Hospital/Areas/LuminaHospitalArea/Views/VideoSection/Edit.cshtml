@* @model VideoSectionEditVM

@{
    ViewData["Title"] = "Edit";
}

<div class="container">
    <div class="row">
        <div class="col-md-6 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Edit Video</h4>

                    <form class="forms-sample" method="post" enctype="multipart/form-data">
                        <input type="hidden" asp-for="Id" />

                        <div class="form-group">
                            <label asp-for="VideoFile"></label>
                            <input asp-for="VideoFile" class="form-control" placeholder="Photos">
                            <span class="text-danger" asp-validation-for="VideoFile"></span>
                        </div>

                        @foreach (var item in Model.VideoTitles)
                        {
                            <div class="form-group">
                                <label asp-for="@item.Title"></label>
                                <input asp-for="@item.Title" class="form-control" placeholder="Photos">
                                <span class="text-danger" asp-validation-for="@item.Title"></span>
                            </div>
                        }

                        <button type="submit" class="btn btn-primary mr-2">Save</button>

                        <a class="btn btn-light" asp-action="Index">Back to List</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    <partial name="_ValidationPartial"></partial>
}

 *@