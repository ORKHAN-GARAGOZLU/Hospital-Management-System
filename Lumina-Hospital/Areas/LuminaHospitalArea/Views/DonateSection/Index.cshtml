@model DonateSectionEditVM
@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Donate Section</h4>
                    <div class="table-responsive pt-3">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>
                                        Title
                                    </th>
                                    <th>
                                        Description
                                    </th>
                                    <th>
                                        Settings
                                    </th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>
                                        @(Model.Title.Length > 30 ? Model.Title.Substring(0, 30) + "..." : Model.Title)
                                    </td>
                                    <td>
                                        @(Model.Description.Length > 30 ? Model.Description.Substring(0, 30) + "..." : Model.Description)
                                    </td>
                                    <td>
                                        <a asp-controller="DonateSection" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-outline-danger">
                                            Edit
                                        </a>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

